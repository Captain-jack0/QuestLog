# Used by `supabase functions serve --env-file supabase/functions/.env.local`
# Copy to .env.local (gitignored) and fill in. Never commit real keys.
RESEND_API_KEY=
UNSUBSCRIBE_SECRET=local-dev-secret
DIGEST_FROM=QuestLog <digest@example.com>
